[sop]
name = "recebi-resolve-review"
description = "Inspect and disposition one exact unpaid Recebi anomaly behind a durable operator-only approval gate."
version = "1.0.0"
priority = "high"
execution_mode = "auto"
cooldown_secs = 0
max_concurrent = 1
deterministic = false
agent = "hickson"

[[triggers]]
type = "manual"
